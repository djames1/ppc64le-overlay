--- a/projects/unix/Makefile	2019-02-20 11:06:41.741492555 -0500
+++ b/projects/unix/Makefile	2019-02-19 21:20:06.172573143 -0500
@@ -89,6 +89,14 @@
   NO_ASM := 1
   $(warning Architecture "$(HOST_CPU)" not officially supported.)
 endif
+ifneq ("$(filter ppc64le powerpc64le,$(HOST_CPU))","")
+  CPU := PPC
+  ARCH_DETECTED := 64BITS
+  BIG_ENDIAN := 0
+  PIC ?= 1
+  NO_ASM := 1
+  $(warning Architecture "$(HOST_CPU)" not officially supported.)
+endif
 ifneq ("$(filter arm%,$(HOST_CPU))","")
   ifeq ("$(filter arm%b,$(HOST_CPU))","")
     CPU := ARM
